// Layout para páginas de detalle — sin BottomNav.
// El BottomNav se reemplaza por una FAB de acción contextual en cada página.
export default function EmpresaDetailLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
